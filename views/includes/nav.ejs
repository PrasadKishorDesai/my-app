<div>
    <nav class="navbar navbar-expand-lg bg-info navbar-dark navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/api/students">Home</a>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="navbar-brand nav-link" href="/admin/students">
                            Add Student
                        </a>
                    </li>
                </ul>
            </div>
            <% if (isAuthenticated) { %>
                <a class="navbar-brand" href="/auth/login">Login</a>
                <a class="navbar-brand" href="/auth/signup">Signup</a>
            <% } else {%>
                <form action="/auth/logout" method="post">
                    <!-- <a class="navbar-brand" href="/auth/logout">Logout</a> -->
                    <button type="submit">Logout</button>
                </form>
            <% } %>
        </div>
    </nav>
</div>