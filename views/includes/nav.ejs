<div>
    <nav class="navbar navbar-expand-lg bg-info navbar-dark navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Home</a>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="navbar-brand nav-link" href="/admin/students">
                            Add Student
                        </a>
                    </li>
                </ul>
            </div>

            <!--
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="navbar-brand" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="navbar-brand" href="/auth/signup">Signup</a>
                    </li>
                    <li class="nav-item">
                        <a class="navbar-brand" href="/auth/signup">
                            <form action="/auth/logout" method="post" style="text-decoration: none;">
                                <button type="submit" style="text-decoration: none;">Logout</button>
                            </form>
                        </a>
                    </li>
                </ul>
            </div>
        -->
            <% if (!isAuthenticated) { %>
                <a class="navbar-brand" href="/auth/login">Login</a>
                <a class="navbar-brand" href="/auth/signup">Signup</a>
            <% } else {%>
                <a class="navbar-brand" href="/auth/signup">
                    <form action="/auth/logout" method="post" style="text-decoration: none;">
                        <!-- <a class="navbar-brand" href="/auth/logout">Logout</a>  -->
                        <button type="submit" style="all: unset;">Logout</button>
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    </form>
                </a>
            <% } %>
        </div>
    </nav>
</div>